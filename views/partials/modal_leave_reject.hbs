<div class="modal fade" id="rejectModal" tabindex="-1" role="dialog" aria-labelledby="rejectModalLabel"
  aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content professional-modal">
      <div class="modal-header">
        <div class="modal-title-section">
          <i class="fa fa-times-circle modal-icon"></i>
          <h4 class="modal-title">Reject Leave Request</h4>
        </div>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
            aria-hidden="true">&times;</span></button>
      </div>
      <form method="POST" action="/requests/reject">
        <div class="modal-body">
          <input type="hidden" value="" name="request">
          <div class="form-group">
            <label for="rejectComment" class="form-label">
              <i class="fa fa-comment"></i> Rejection Comment:
            </label>
            <textarea class="form-control modal-textarea" id="rejectComment" name="comment" rows="3" required
              placeholder="Please provide a reason for rejecting this leave request..."></textarea>
            <p class="form-help"><em>Please enter a comment explaining why this request is being rejected
                (required)</em></p>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">
            <i class="fa fa-times"></i> Cancel
          </button>
          <button type="submit" class="btn btn-danger">
            <i class="fa fa-ban"></i> Reject Request
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
  .professional-modal {
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    border: none;
  }

  .professional-modal .modal-header {
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    border-bottom: 1px solid #dee2e6;
    padding: 25px 30px;
    border-radius: 12px 12px 0 0;
  }

  .modal-title-section {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .modal-icon {
    font-size: 1.8rem;
    color: #dc3545;
  }

  .professional-modal .modal-title {
    font-size: 2.2rem;
    font-weight: 600;
    color: var(--text-color);
    margin: 0;
  }

  .professional-modal .modal-body {
    padding: 30px;
  }

  .form-label {
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--text-color);
    margin-bottom: 12px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .form-label i {
    color: #dc3545;
    font-size: 1.2rem;
  }

  .modal-textarea {
    font-size: 1.3rem;
    padding: 15px 18px;
    border-radius: 8px;
    border: 2px solid #dee2e6;
    transition: border-color 0.3s ease;
    resize: vertical;
    min-height: 80px;
  }

  .modal-textarea:focus {
    border-color: #dc3545;
    box-shadow: 0 0 0 0.2rem rgba(220, 53, 69, 0.25);
    outline: none;
  }

  .form-help {
    font-size: 1.1rem;
    color: var(--hover-text-color);
    margin-top: 8px;
    margin-bottom: 0;
  }

  .professional-modal .modal-footer {
    background: var(--secondary-background-color);
    border-top: 1px solid #dee2e6;
    padding: 20px 30px;
    border-radius: 0 0 12px 12px;
  }

  .professional-modal .btn {
    font-size: 1.2rem;
    font-weight: 500;
    padding: 12px 24px;
    border-radius: 6px;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: all 0.3s ease;
  }

  .btn-outline-secondary {
    background: transparent;
    color: var(--hover-text-color);
    border: 2px solid #6c757d;
  }

  .btn-outline-secondary:hover {
    background: #6c757d;
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(108, 117, 125, 0.3);
  }

  .btn-danger {
    background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
    border: none;
    color: white;
  }

  .btn-danger:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(220, 53, 69, 0.4);
    color: white;
  }

  .close {
    font-size: 1.8rem;
    color: var(--hover-text-color);
    opacity: 0.8;
    transition: opacity 0.3s ease;
  }

  .close:hover {
    opacity: 1;
    color: #dc3545;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .professional-modal .modal-title {
      font-size: 1.8rem;
    }

    .form-label {
      font-size: 1.2rem;
    }

    .modal-textarea {
      font-size: 1.1rem;
    }

    .professional-modal .btn {
      font-size: 1.1rem;
      padding: 10px 20px;
    }
  }
</style>
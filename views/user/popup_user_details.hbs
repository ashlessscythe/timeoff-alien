<div>
  <strong>Name:</strong> {{userFullName}}.
</div>
<div>
  <strong>Department:</strong> {{departmentName}}.
</div>
{{#if userAllowance }}
<div>
  <strong>Available:</strong> [{{format_days_and_hours userAllowance.remaining_regular_allowance}} reg,
  {{#if userAllowance.total_personal_available}}{{format_days_and_hours userAllowance.total_personal_available}}{{else}}0 days{{/if}} pers]
  out of
  {{format_days_and_hours userAllowance.total_number_of_days_in_allowance}} total.
</div>
{{#if (lt userAllowance.number_of_days_available_in_allowance 0)}}
<div class="text-danger">
  <strong>⚠️ Overbooked!</strong> Negative balance - user must cancel requests before approval.
</div>
{{/if}}
{{/if}}
{{#if specialLeaveTypes}}
<div>
  <strong>Special Leave Days Taken:</strong>
  <ul>
    {{#each specialLeaveTypes}}
    <li>{{this.name}}: {{format_days_and_hours this.days_taken}}{{#unless
      @last}},{{/unless}}</li>
    {{/each}}
  </ul>
</div>
{{/if}}
{{#if supervisorNames}}
<div>
  <strong>Approvers:</strong>
  <ul>
    {{#each supervisorNames}}<li>{{this}}{{#unless @last}},{{/unless}}</li>{{/each}}
  </ul>
</div>
{{/if}}